function solution(num_list) {
    let num1 = 0;
    let num2 = 0;
    let arr = [];
    
    for(let i=0; i<num_list.length; i++){
        if (num_list[i]%2 === 0){
            num1 ++;
        } else {
            num2 ++;
        } 
    }
    arr.push(num1)
    arr.push(num2)
    return (arr)
}